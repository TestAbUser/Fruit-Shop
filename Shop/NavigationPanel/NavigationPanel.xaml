<UserControl x:Class="Shop.NavigationPanel.NavigationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shop.NavigationPanel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!--<ItemsControl ItemsSource="{Binding PageViewModels}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding PageName}"
                            Command="{Binding DataContext.SwitchPageCommand,RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                            CommandParameter="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>-->
    <Grid>
        <Grid Background="Black">
            <Grid HorizontalAlignment="Left"
                  Width="65">
                <StackPanel Name="Navigation">
                    <Grid Height="90">
                        <ToggleButton Grid.Column="0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Height="30"
                                      Width="30"
                                      Margin="18,-20,0,0">
                            <ToggleButton.Background>
                                <ImageBrush ImageSource="/Images/hamb-icon.png" 
                                            Stretch="Fill"/>
                            </ToggleButton.Background>
                            <ToggleButton.Triggers>
                                <EventTrigger RoutedEvent="ToggleButton.Unchecked">
                                    <BeginStoryboard>
                                        <Storyboard x:Name="ShowPanel">
                                            <DoubleAnimation
                                                Storyboard.TargetName="Navigation"
                                                Storyboard.TargetProperty="Width"
                                                BeginTime="0:0:0"
                                                From="65" To="230"
                                                Duration="0">

                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard> 
                                </EventTrigger>
                            </ToggleButton.Triggers>
                        </ToggleButton>
                       

                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
